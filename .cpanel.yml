---
deployment:
  tasks:
    - export DEPLOYPATH=/home/c2554004c/repositories/BackendDeliveryApp 
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/rsync -az --delete --exclude=".git" --exclude="node_modules" ./ $DEPLOYPATH

<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title>Tableau de Bord Restaurant - Gourmet d'amour</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Poppins:wght@300;400;600&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        :root {
            --primary-color: #DB607E;
            --secondary-color: #ABD27E;
            --accent-color: #b22222;
            --background-color: #f9f5f0;
            --dark-color: #2c2c2c;
            --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --hover-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--background-color);
            min-height: 100vh;
            color: var(--dark-color);
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background: rgba(0, 0, 0, 0.4);
        }

        .modal-content {
            background: #fff;
            margin: 10% auto;
            padding: 20px;
            border-radius: 8px;
            width: 90%;
            max-width: 500px;
            position: relative;
        }

        .close {
            position: absolute;
            right: 20px;
            top: 10px;
            font-size: 28px;
            cursor: pointer;
        }

        .dashboard-container {
            display: flex;
            min-height: 100vh;
        }

        .sidebar {
            width: 250px;
            background: white;
            padding: 1rem;
            box-shadow: var(--card-shadow);
            position: fixed;
            height: 100vh;
            z-index: 100;
        }

        .logo-icon {
            width: 150px;
            height: 150px
        }

        .sidebar-header {
            margin-bottom: 2rem;
            text-align: center;
        }

        .sidebar-header h1 {
            font-family: 'Playfair Display', serif;
            color: var(--primary-color);
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
        }

        .nav-menu {
            list-style: none;
        }

        .nav-item {
            margin-bottom: 0.5rem;
        }

        .nav-link {
            display: flex;
            align-items: center;
            padding: 0.75rem 1rem;
            color: var(--dark-color);
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .nav-link:hover,
        .nav-link.active {
            background-color: var(--primary-color);
            color: white;
        }

        .nav-link i {
            margin-right: 0.75rem;
            width: 20px;
            text-align: center;
        }

        .main-content {
            flex: 1;
            margin-left: 250px;
            padding: 2rem;
        }

        .page-header {
            margin-bottom: 2rem;
        }

        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            color: var(--dark-color);
            margin-bottom: 0.5rem;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: white;
            padding: 1.5rem;
            border-radius: 15px;
            box-shadow: var(--card-shadow);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: var(--primary-color);
        }

        .stat-card h3 {
            font-size: 1.1rem;
            color: var(--dark-color);
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
        }

        .stat-card h3 i {
            margin-right: 0.5rem;
            color: var(--primary-color);
        }

        .stat-card p {
            font-size: 2.5rem;
            font-weight: 600;
            color: var(--primary-color);
        }

        .chart-container {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: var(--card-shadow);
            margin-bottom: 2rem;
            height: 400px;
            position: relative;
        }

        .chart-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--dark-color);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
        }

        .chart-title i {
            margin-right: 0.75rem;
            color: var(--primary-color);
        }

        .orders-section {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: var(--card-shadow);
        }

        .orders-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .orders-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--dark-color);
            display: flex;
            align-items: center;
        }

        .orders-title i {
            margin-right: 0.75rem;
            color: var(--primary-color);
        }

        .orders-table {
            width: 100%;
            border-collapse: collapse;
        }

        .orders-table th {
            text-align: left;
            padding: 1rem;
            border-bottom: 2px solid var(--primary-color);
            color: var(--dark-color);
            font-weight: 600;
        }

        .orders-table td {
            padding: 1rem;
            border-bottom: 1px solid #eee;
        }

        .orders-table tr:hover {
            background-color: var(--background-color);
            cursor: pointer;
        }

        .status-badge {
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.875rem;
            font-weight: 500;
            display: inline-block;
        }

        .status-pending {
            background-color: #f3f4f6;
            color: #4b5563;
        }

        .status-ready {
            background-color: #e0f2f1;
            color: #00695c;
        }

        .status-preparing {
            background-color: #dbeafe;
            color: #1e40af;
        }

        .status-delivering {
            background-color: #fef3c7;
            color: #92400e;
        }

        .status-completed {
            background-color: #dcfce7;
            color: #166534;
        }

        .status-cancelled {
            background-color: #fee2e2;
            color: #991b1b;
        }

        .action-dropdown {
            position: relative;
            display: inline-block;
        }

        .action-button {
            background-color: #f3f4f6;
            color: var(--dark-color);
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
        }

        .action-button:hover {
            background-color: #e5e7eb;
        }

        .dropdown-menu {
            position: absolute;
            right: 0;
            top: 100%;
            background: white;
            border-radius: 8px;
            box-shadow: var(--card-shadow);
            min-width: 200px;
            z-index: 1000;
            display: none;
        }

        .dropdown-menu.show {
            display: block;
        }

        .dropdown-item {
            padding: 0.75rem 1rem;
            color: var(--dark-color);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
        }

        .dropdown-item:hover {
            background-color: var(--background-color);
        }

        .dropdown-item i {
            width: 20px;
            text-align: center;
        }

        .note-container {
            position: relative;
        }

        .note-text {
            display: block;
            padding: 0.5rem;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }

        .note-text:hover {
            background-color: var(--background-color);
        }

        .note-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000;
        }

        .note-modal-content {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: white;
            padding: 2rem;
            border-radius: 8px;
            width: 90%;
            max-width: 500px;
        }

        .note-modal textarea {
            width: 100%;
            min-height: 100px;
            padding: 0.75rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            margin-bottom: 1rem;
            font-family: inherit;
        }

        .note-modal-buttons {
            display: flex;
            justify-content: flex-end;
            gap: 1rem;
        }

        @media (max-width: 768px) {
            .sidebar {
                width: 70px;
                padding: 1rem;
            }

            .sidebar-header h1 {
                display: none;
            }

            .nav-link span {
                display: none;
            }

            .nav-link i {
                margin-right: 0;
                font-size: 1.2rem;
            }

            .main-content {
                margin-left: 70px;
                padding: 1rem;
            }

            .stats-grid {
                grid-template-columns: 1fr;
            }

            .orders-table {
                display: block;
                overflow-x: auto;
            }
        }

        .search-container {
            position: relative;
            width: 300px;
        }

        .search-input {
            width: 100%;
            padding: 0.75rem 1rem;
            padding-right: 2.5rem;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

        .search-input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 2px rgba(219, 96, 126, 0.1);
        }

        .search-icon {
            position: absolute;
            right: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: #666;
        }

        .pagination-container {
            margin-top: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
        }

        .pagination-info {
            color: #666;
            font-size: 0.9rem;
        }

        .pagination-controls {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .pagination-btn {
            padding: 0.5rem 1rem;
            border: 1px solid #ddd;
            border-radius: 6px;
            background: white;
            color: var(--dark-color);
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .pagination-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .pagination-btn:not(:disabled):hover {
            background: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
        }

        .page-numbers {
            display: flex;
            gap: 0.5rem;
        }

        .page-number {
            width: 32px;
            height: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid #ddd;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .page-number.active {
            background: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
        }

        .page-number:hover:not(.active) {
            background: var(--background-color);
        }

        .nav-item.logout {
            margin-top: auto;
            color: #dc3545;
        }

        .nav-item.logout:hover {
            background-color: #dc3545;
            color: white;
        }

        .sidebar-nav {
            display: flex;
            flex-direction: column;
            height: calc(100% - 60px);
        }

        .stat-card small {
            font-size: 0.8em;
            color: #666;
            margin-left: 5px;
        }

        .rating-display {
            display: flex;
            align-items: baseline;
            justify-content: center;
            gap: 2px;
            white-space: nowrap;
        }

        .rating-value {
            font-size: 2.0em;
            font-weight: bold;
            color: #2c3e50;
            line-height: 1;
        }

        .rating-max {
            font-size: 1.2em;
            color: #7f8c8d;
            line-height: 1;
        }

        .rating-count {
            font-size: 0.8em;
            color: #95a5a6;
            margin-left: 8px;
            line-height: 1;
        }

        .stat-card p {
            color: #2c3e50;
            font-size: 1.8em;
            font-weight: bold;
            margin: 0;
            line-height: 1;
            display: flex;
            align-items: baseline;
            justify-content: center;
        }
    </style>
</head>

<body>
    <div class="dashboard-container">
        <aside class="sidebar">
            <div class="sidebar-header">
                <img src="/images/logo.png" alt="logo" class="logo-icon">
                <h1>Gourmet d'amour</h1>
                <p>Restaurant</p>
            </div>
            <nav>
                <ul class="nav-menu">
                    <li class="nav-item">
                        <a href="/admin/restaurant/<%= restaurant.id %>/dashboard" class="nav-link active">
                            <i class="fas fa-chart-line"></i>
                            <span>Tableau de Bord</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/restaurant/<%= restaurant.id %>/menu" class="nav-link">
                            <i class="fas fa-utensils"></i>
                            <span>Menu</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/restaurant/logout" class="nav-link logout">
                            <i class="fas fa-sign-out-alt"></i>
                            <span>Déconnexion</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>

        <main class="main-content">
            <div class="page-header">
                <h2 class="page-title">Tableau de Bord</h2>
                <p>Bienvenue, <%= restaurant.name %>
                </p>
            </div>

            <div class="stats-grid">
                <div class="stat-card">
                    <h3><i class="fas fa-money-bill-wave"></i> Revenu Total</h3>
                    <p>
                        <%= totalRevenue.toLocaleString() %> Fcfa
                    </p>
                </div>

                <div class="stat-card">
                    <h3><i class="fas fa-calendar-day"></i> Revenu Aujourd'hui</h3>
                    <p>
                        <%= todayRevenue.toLocaleString() %> Fcfa
                    </p>
                </div>

                <div class="stat-card">
                    <h3><i class="fas fa-users"></i> Clients Aujourd'hui</h3>
                    <p id="todayClientsCount">
                        <%= orders.filter(order=> new Date(order.created_at).toLocaleDateString() === new
                            Date().toLocaleDateString()).length %>
                    </p>
                </div>


                <div class="stat-card">
                    <h3><i class="fas fa-shopping-cart"></i> Commandes Totales</h3>
                    <p id="totalOrdersCount">
                        <%= orders.length %>
                    </p>
                </div>

                <div class="stat-card">
                    <h3><i class="fas fa-check-circle"></i> Comm. Complètes</h3>
                    <p>
                        <%= completedOrders %>
                    </p>
                </div>

                <div class="stat-card">
                    <h3><i class="fas fa-hourglass-half"></i> Commandes En Cours</h3>
                    <p>
                        <%= uncompletedOrders %>
                    </p>
                </div>

                <div class="stat-card">
                    <h3><i class="fas fa-calendar-day"></i> Comm. Aujourd'hui</h3>
                    <p id="todayOrdersCount">
                        <%= orders.filter(order=> new Date(order.created_at).toLocaleDateString() === new
                            Date().toLocaleDateString()).length %>
                    </p>
                </div>

                <div class="stat-card">
                    <h3><i class="fas fa-star"></i> Note Moyenne</h3>
                    <p id="averageRating" class="rating-display">
                        <% const ratedOrders=orders.filter(order=> order.rating);
                            const averageRating = ratedOrders.length > 0
                            ? (ratedOrders.reduce((sum, order) => sum + order.rating, 0) /
                            ratedOrders.length).toFixed(1)
                            : 0;
                            %>
                            <span class="rating-value">
                                <%= averageRating %>
                            </span>
                            <span class="rating-max">/5</span>
                            <span class="rating-count">(<%= ratedOrders.length %> avis)</span>
                    </p>
                </div>
            </div>

            <div class="chart-container">
                <h3 class="chart-title">
                    <i class="fas fa-chart-bar"></i>
                    Revenus Mensuels
                </h3>
                <canvas id="revenueChart"></canvas>
            </div>

            <div class="orders-section">
                <div class="orders-header">
                    <h3 class="orders-title">
                        <i class="fas fa-list"></i>
                        Liste des Commandes
                    </h3>
                    <div class="search-container">
                        <input type="text" id="searchInput" placeholder="Rechercher par nom ou adresse..."
                            class="search-input">
                        <i class="fas fa-search search-icon"></i>
                    </div>
                </div>

                <table class="orders-table">
                    <thead>
                        <tr>
                            <th>N°</th>
                            <th>Date</th>
                            <th>Client</th>
                            <th>Téléphone</th>
                            <th>Adresse</th>
                            <th>Revenu</th>
                            <th>Livraison</th>
                            <th>Note</th>
                            <th>Statut</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="ordersTableBody">
                        <% if (orders && orders.length> 0) { %>
                            <% orders.forEach((order, index)=> { %>
                                <tr class="order-row" data-order='<%= JSON.stringify(order) %>'
                                    data-order-id="<%= order.id %>">
                                    <td>
                                        <%= index + 1 %>
                                    </td>
                                    <td>
                                        <%= new Date(order.created_at).toLocaleString() %>
                                    </td>
                                    <td>
                                        <%= order.user?.name || 'N/A' %>
                                    </td>
                                    <td>
                                        <%= order.user?.phone_number || 'N/A' %>
                                    </td>
                                    <td>
                                        <%= order.delivery_address?.address || 'N/A' %>
                                    </td>
                                    <td>
                                        <%= order.items_total.toLocaleString() %> F
                                    </td>
                                    <td>
                                        <%= order.delivery_fee_final.toLocaleString() %> F
                                    </td>
                                    <td>
                                        <div class="note-container">
                                            <span class="note-text">
                                                <%= order.rating || 0 %>/5
                                            </span>
                                        </div>
                                    </td>
                                    <td>
                                        <span class="status-badge status-<%= order.status.toLowerCase() %>">
                                            <%= order.status==='PENDING' ? 'En attente' : order.status==='PREPARING'
                                                ? 'En préparation' : order.status==='DELIVERING' ? 'En livraison' :
                                                order.status==='COMPLETED' ? 'Livrée' : 'Annulée' %>
                                        </span>
                                    </td>
                                    <td>
                                        <div class="action-dropdown">
                                            <button class="action-button" type="button">
                                                <span>Actions</span>
                                                <i class="fas fa-chevron-down"></i>
                                            </button>
                                            <div class="dropdown-menu">
                                                <a href="#" class="dropdown-item" data-action="PREPARING"> <i
                                                        class="fas fa-sync"></i> En préparation </a>
                                                <a href="#" class="dropdown-item" data-action="READY">
                                                    <i class="fas fa-bell"></i> Prête
                                                </a>
                                                <a href="#" class="dropdown-item" data-action="DELIVERING"> <i
                                                        class="fas fa-truck"></i> En livraison </a>
                                                <a href="#" class="dropdown-item" data-action="COMPLETED"> <i
                                                        class="fas fa-check"></i> Livrée </a>
                                                <a href="#" class="dropdown-item" data-action="CANCELLED"> <i
                                                        class="fas fa-times"></i> Annuler </a>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                                <% }); %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="9" style="text-align: center; padding: 2rem;">
                                                Aucune commande trouvée
                                            </td>
                                        </tr>
                                        <% } %>
                    </tbody>
                </table>

                <div class="pagination-container">
                    <div class="pagination-info">
                        Affichage de <span id="startIndex">1</span> à <span id="endIndex">10</span> sur <span
                            id="totalOrders">
                            <%= orders.length %>
                        </span> commandes
                    </div>
                    <div class="pagination-controls">
                        <button id="prevPage" class="pagination-btn" disabled>
                            <i class="fas fa-chevron-left"></i> Précédent
                        </button>
                        <div id="pageNumbers" class="page-numbers"></div>
                        <button id="nextPage" class="pagination-btn">
                            Suivant <i class="fas fa-chevron-right"></i>
                        </button>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script async defer 
        src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCDVi5RrYOFaIP0pV4RJr26X8X8LI_1fZY">
    </script>

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        function renderOrderMap(order) {
            if (!order.delivery_address?.latitude || !order.delivery_address?.longitude) return;

            const lat = order.delivery_address.latitude;
            const lng = order.delivery_address.longitude;

            const mapDiv = document.getElementById('orderMap');
            mapDiv.innerHTML = ""; // Reset map

            const map = new google.maps.Map(mapDiv, {
                zoom: 14,
                center: { lat, lng },
            });

            new google.maps.Marker({
                position: { lat, lng },
                map: map,
                title: "Adresse du client",
            });
        }
        function openOrderModal(order) {
            const modal = document.getElementById('orderDetailsModal');
            const content = document.getElementById('orderDetailsContent');

            let itemsHTML = '';

            if (order.items && order.items.length > 0) {
                itemsHTML = `
                    <h3 style="margin-bottom:10px;">Articles commandés</h3>
                    <ul style="margin-top:10px; list-style:none; padding:0;">
                        ${
                            order.items.map(item => {
                                const img = item.menuItem?.imageUrl 
                                            || item.menuItem?.image_url;

                                const name = item.menuItem?.name || "Article";
                                const desc = item.menuItem?.description || "";
                                console.log(item)

                                return `
                                    <li style="display:flex; gap:12px; margin-bottom:15px; align-items:flex-start;">
                                        <img src="${img}" 
                                            style="width:70px; height:70px; border-radius:8px; object-fit:cover;">
                                        
                                        <div>
                                            <strong>${name}</strong><br>
                                            <span>${item.quantity} × ${item.price.toLocaleString()} F</span><br>
                                            <small style="color:#555">${desc}</small>
                                        </div>
                                    </li>
                                `;
                            }).join('')
                        }
                    </ul>
                `;
            } else {
                itemsHTML = "<p>Aucun article trouvé.</p>";
            }

            content.innerHTML = `
                <p><strong>Client:</strong> ${order.user?.name || 'N/A'}</p>
                <p><strong>Adresse:</strong> ${order.delivery_address?.address || 'N/A'}</p>
                <p><strong>Total Produits:</strong> ${order.items_total.toLocaleString()} Fcfa</p>
                <p><strong>Frais de Livraison:</strong> ${order.delivery_fee_final.toLocaleString()} Fcfa</p>
                <hr style="margin:1em 0;">
                ${itemsHTML}
            `;

            modal.style.display = "block";

            renderOrderMap(order);
        }


        function closeOrderModal() {
            document.getElementById('orderDetailsModal').style.display = 'none';
        }
        // Close modal on outside click
        window.addEventListener("click", function(e) {
            const modal = document.getElementById('orderDetailsModal');
            if (e.target === modal) {
                closeOrderModal();
            }
        });

        // Force console logs to appear

        document.addEventListener('DOMContentLoaded', function () {
            // Get all orders from the table
            const orderRows = document.querySelectorAll('.order-row');

            const orders = Array.from(orderRows).map(row => {
                try {
                    const orderData = JSON.parse(row.dataset.order);
                    // Ensure items_total is a number and handle different formats
                    if (typeof orderData.items_total === 'string') {
                        // Remove any currency symbols and spaces
                        orderData.items_total = parseFloat(orderData.items_total.replace(/[^0-9.-]+/g, '')) || 0;
                    } else {
                        orderData.items_total = parseFloat(orderData.items_total) || 0;
                    }
                    return orderData;
                } catch (e) {
                    console.error('Error parsing order data:', e);
                    return null;
                }
            }).filter(order => order !== null);

            const itemsPerPage = 10;
            let currentPage = 1;
            let filteredOrders = [...orders];

            console.log(orders)

            attachOrderDetailsListeners();

            function attachOrderDetailsListeners() {
                const tableBody = document.getElementById("ordersTableBody");

                tableBody.addEventListener("click", (e) => {
                    // Ignore buttons/dropdowns
                    if (e.target.closest('.action-dropdown')) return;

                    let row = e.target.closest("tr[data-order-id]");
                    if (!row) return;

                    const orderData = row.getAttribute("data-order");
                    if (!orderData) return;

                    const order = JSON.parse(orderData);
                    openOrderModal(order);
                });
            }



            // Update stats function
            function updateStats(orders) {
                const today = new Date().toLocaleDateString();

                const todayOrders = orders.filter(order => {
                    const orderDate = new Date(order.created_at).toLocaleDateString();
                    return orderDate === today;
                });

                const totalRevenue = orders.reduce((total, order) => {
                    const amount = parseFloat(order.items_total) || 0;
                    return total + amount;
                }, 0);

                const todayRevenue = todayOrders.reduce((total, order) => {
                    const amount = parseFloat(order.items_total) || 0;
                    return total + amount;
                }, 0);

                // Calculate average rating
                const ratedOrders = orders.filter(order => order.rating);
                const averageRating = ratedOrders.length > 0
                    ? (ratedOrders.reduce((sum, order) => sum + order.rating, 0) / ratedOrders.length).toFixed(1)
                    : 0;

                document.getElementById('totalOrdersCount').textContent = orders.length;
                document.getElementById('todayOrdersCount').textContent = todayOrders.length;
                document.getElementById('todayClientsCount').textContent = todayOrders.length;
                document.getElementById('todayRevenue').textContent = `${todayRevenue.toLocaleString()} Fcfa`;
                document.getElementById('totalRevenue').textContent = `${totalRevenue.toLocaleString()} Fcfa`;
                document.getElementById('averageRating').innerHTML = `
                    <span class="rating-value">${averageRating}</span>
                    <span class="rating-max">/5</span>
                    <span class="rating-count">(${ratedOrders.length} avis)</span>
                `;
            }

            // 🔹 Attach click on X (close button) for order details modal
            const orderModal = document.getElementById('orderDetailsModal');
            const closeBtn = orderModal ? orderModal.querySelector('.close') : null;

            if (closeBtn) {
                closeBtn.addEventListener('click', function (e) {
                    e.stopPropagation(); // prevent window click handler from interfering
                    closeOrderModal();
                });
            }

            // Search functionality
            const searchInput = document.getElementById('searchInput');
            if (searchInput) {
                searchInput.addEventListener('input', function (e) {
                    const searchTerm = e.target.value.toLowerCase();

                    filteredOrders = orders.filter(order => {
                        const userName = (order.user?.name || '').toLowerCase();
                        const address = (order.delivery_address?.address || '').toLowerCase();
                        const matches = userName.includes(searchTerm) || address.includes(searchTerm);
                        return matches;
                    });

                    currentPage = 1;
                    renderOrders();
                    updatePagination();
                    updateStats(filteredOrders);
                });
            }

            // Pagination functionality
            function renderOrders() {
                const startIndex = (currentPage - 1) * itemsPerPage;
                const endIndex = Math.min(startIndex + itemsPerPage, filteredOrders.length);
                const tbody = document.getElementById('ordersTableBody');
                if (!tbody) return;

                tbody.innerHTML = '';

                if (filteredOrders.length === 0) {
                    tbody.innerHTML = `
                        <tr>
                            <td colspan="9" style="text-align: center; padding: 2rem;">
                                Aucune commande trouvée
                            </td>
                        </tr>
                    `;
                    return;
                }

                filteredOrders.slice(startIndex, endIndex).forEach((order, index) => {
                    const row = document.createElement('tr');
                    row.classList.add("order-row");
                    row.setAttribute('data-order-id', order.id);
                    row.setAttribute('data-order', JSON.stringify(order));
                    row.innerHTML = `
                        <td>${startIndex + index + 1}</td>
                        <td>${new Date(order.created_at).toLocaleString()}</td>
                        <td>${order.user?.name || 'N/A'}</td>
                        <td>${order.user?.phone_number || 'N/A'}</td>
                        <td>${order.delivery_address?.address || 'N/A'}</td>
                        <td>${order.items_total.toLocaleString()} F</td>
                        <td>${order.delivery_fee_final.toLocaleString()} F</td>
                        <td>
                            <div class="note-container">
                                <span class="note-text">${order.rating || 0}/5</span>
                            </div>
                        </td>
                        <td>
                            <span class="status-badge status-${order.status.toLowerCase()}">
                                ${order.status === 'PENDING' ? 'En attente' :
                            order.status === 'PREPARING' ? 'En préparation' :
                                order.status === 'READY' ? 'Prête' :
                                    order.status === 'DELIVERING' ? 'En livraison' :
                                        order.status === 'COMPLETED' ? 'Livrée' : 'Annulée'}
                            </span>
                        </td>
                        <td>
                            <div class="action-dropdown">
                                <button class="action-button" type="button">
                                    <span>Actions</span>
                                    <i class="fas fa-chevron-down"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a href="#" class="dropdown-item" data-action="PREPARING"> <i class="fas fa-sync"></i> En préparation </a>
                                    <a href="#" class="dropdown-item" 
                                    data-action="READY"> <i class="fas fa-bell"></i> Prête </a>
                                    <a href="#" class="dropdown-item" data-action="DELIVERING"> <i class="fas fa-truck"></i> En livraison </a>
                                    <a href="#" class="dropdown-item" data-action="COMPLETED"> <i class="fas fa-check"></i> Livrée </a>
                                    <a href="#" class="dropdown-item" data-action="CANCELLED"> <i class="fas fa-times"></i> Annuler </a>
                                </div>
                            </div>
                        </td>
                    `;
                    tbody.appendChild(row);
                });

                // Update pagination info
                const startIndexEl = document.getElementById('startIndex');
                const endIndexEl = document.getElementById('endIndex');
                const totalOrdersEl = document.getElementById('totalOrders');

                if (startIndexEl) startIndexEl.textContent = filteredOrders.length === 0 ? 0 : startIndex + 1;
                if (endIndexEl) endIndexEl.textContent = endIndex;
                if (totalOrdersEl) totalOrdersEl.textContent = filteredOrders.length;

                // Attach listeners to new elements
                attachDropdownListeners();
                attachOrderStatusListeners();
            }

            function updatePagination() {
                const totalPages = Math.ceil(filteredOrders.length / itemsPerPage);
                const pageNumbers = document.getElementById('pageNumbers');
                const prevButton = document.getElementById('prevPage');
                const nextButton = document.getElementById('nextPage');

                if (!pageNumbers || !prevButton || !nextButton) return;

                // Update buttons
                prevButton.disabled = currentPage === 1;
                nextButton.disabled = currentPage === totalPages;

                // Update page numbers
                pageNumbers.innerHTML = '';
                const maxPages = 5;
                let startPage = Math.max(1, currentPage - Math.floor(maxPages / 2));
                let endPage = Math.min(totalPages, startPage + maxPages - 1);

                if (endPage - startPage + 1 < maxPages) {
                    startPage = Math.max(1, endPage - maxPages + 1);
                }

                for (let i = startPage; i <= endPage; i++) {
                    const pageNumber = document.createElement('div');
                    pageNumber.className = `page-number ${i === currentPage ? 'active' : ''}`;
                    pageNumber.textContent = i;
                    pageNumber.onclick = () => {
                        currentPage = i;
                        renderOrders();
                        updatePagination();
                    };
                    pageNumbers.appendChild(pageNumber);
                }
            }

            // Event listeners for pagination buttons
            const prevButton = document.getElementById('prevPage');
            const nextButton = document.getElementById('nextPage');

            if (prevButton) {
                prevButton.addEventListener('click', () => {
                    if (currentPage > 1) {
                        currentPage--;
                        renderOrders();
                        updatePagination();
                    }
                });
            }

            if (nextButton) {
                nextButton.addEventListener('click', () => {
                    const totalPages = Math.ceil(filteredOrders.length / itemsPerPage);
                    if (currentPage < totalPages) {
                        currentPage++;
                        renderOrders();
                        updatePagination();
                    }
                });
            }

            // Attach dropdown toggle event listeners (CSP-compliant)
            function attachDropdownListeners() {
                document.querySelectorAll('.action-button').forEach(button => {
                    button.addEventListener('click', function (event) {
                        event.stopPropagation();
                        // Close all other dropdowns first
                        document.querySelectorAll('.dropdown-menu').forEach(menu => menu.classList.remove('show'));
                        const dropdown = button.nextElementSibling;
                        if (dropdown) dropdown.classList.toggle('show');
                    });
                });
            }

            // Attach order status update event listeners (CSP-compliant)
            function attachOrderStatusListeners() {
                document.querySelectorAll('.dropdown-item').forEach(item => {
                    item.addEventListener('click', function (event) {
                        event.preventDefault();
                        const action = item.getAttribute('data-action');
                        const orderRow = item.closest('tr[data-order-id]');
                        const orderId = orderRow ? orderRow.getAttribute('data-order-id') : null;
                        // Close all dropdowns
                        document.querySelectorAll('.dropdown-menu').forEach(menu => menu.classList.remove('show'));
                        if (action && orderId) {
                            updateOrderStatus(orderId, action);
                        }
                    });
                });
            }

            // Close dropdowns when clicking outside
            document.addEventListener('click', function (event) {
                document.querySelectorAll('.dropdown-menu').forEach(menu => menu.classList.remove('show'));
            });

            // Initial render
            renderOrders();
            updatePagination();
            updateStats(orders);
        });

        // Check authentication on page load
        document.addEventListener('DOMContentLoaded', function () {
            const token = localStorage.getItem('token');
            if (!token) {
                window.location.href = '/admin/login/restaurant';
                return;
            }

            // Add token to all fetch requests
            const originalFetch = window.fetch;
            window.fetch = function (url, options = {}) {
                if (!options.headers) {
                    options.headers = {};
                }
                options.headers['Authorization'] = `Bearer ${token}`;
                return originalFetch(url, options);
            };
        });

        // Function to get CSRF token from cookie
        function getCSRFToken() {
            const name = '_csrf=';
            const decodedCookie = decodeURIComponent(document.cookie);
            const cookieArray = decodedCookie.split(';');
            for (let i = 0; i < cookieArray.length; i++) {
                let cookie = cookieArray[i];
                while (cookie.charAt(0) === ' ') {
                    cookie = cookie.substring(1);
                }
                if (cookie.indexOf(name) === 0) {
                    return cookie.substring(name.length, cookie.length);
                }
            }
            return '';
        }

        // Function to make GraphQL requests
        async function makeGraphQLRequest(query, variables = {}) {
            const token = localStorage.getItem('token');
            if (!token) {
                window.location.href = '/admin/login/restaurant';
                return;
            }

            const response = await fetch("/graphql", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json",
                    "Authorization": `Bearer ${token}`,
                    "Accept": "application/json",
                    "X-CSRF-Token": getCSRFToken()
                },
                body: JSON.stringify({ query, variables }),
            });

            if (!response.ok) {
                if (response.status === 401) {
                    window.location.href = '/admin/login/restaurant';
                    return;
                }
                throw new Error(`HTTP error! Status: ${response.status}`);
            }

            const result = await response.json();
            if (result.errors) {
                throw new Error(result.errors[0].message);
            }

            return result.data;
        }

        // Revenue Chart
        const revenueCtx = document.getElementById('revenueChart').getContext('2d');

        // Safely pass your data from EJS → JS
        const monthlyRevenueData = JSON.parse('<%- JSON.stringify(monthlyRevenue || []) %>');
        const months = monthlyRevenueData.map(r => r.month);
        const revenues = monthlyRevenueData.map(r => Number(r.revenue));

        new Chart(revenueCtx, {
            type: 'line',
            data: {
                labels: months.length > 0 ? months : ['Aucun mois'],
                datasets: [{
                    label: 'Revenus',
                    data: revenues.length > 0 ? revenues : [0],
                    backgroundColor: 'rgba(219, 96, 126, 0.1)',
                    borderColor: 'rgba(219, 96, 126, 1)',
                    borderWidth: 2,
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    },
                    tooltip: {
                        backgroundColor: 'rgba(44, 44, 44, 0.9)',
                        titleFont: {
                            family: "'Poppins', sans-serif",
                            size: 14
                        },
                        bodyFont: {
                            family: "'Poppins', sans-serif",
                            size: 13
                        },
                        padding: 12,
                        cornerRadius: 8,
                        displayColors: false,
                        callbacks: {
                            label: function (context) {
                                return `${context.parsed.y.toLocaleString()} Fcfa`;
                            }
                        }
                    }
                },
                scales: {
                    x: {
                        grid: { display: false },
                        title: {
                            display: true,
                            text: "Mois",
                            color: "#2c2c2c",
                            font: { size: 14, family: "'Poppins', sans-serif", weight: '600' },
                            padding: { top: 10 }
                        }
                    },
                    y: {
                        beginAtZero: true,
                        grid: { color: 'rgba(0, 0, 0, 0.05)' },
                        title: {
                            display: true,
                            text: "Revenus (Fcfa)",
                            color: "#2c2c2c",
                            font: { size: 14, family: "'Poppins', sans-serif", weight: '600' },
                            padding: { bottom: 10 }
                        }
                    }
                }
            }
        });

        // Orders Chart
        const ordersCtx = document.getElementById('ordersChart').getContext('2d');
        new Chart(ordersCtx, {
            type: 'bar',
            data: {
                labels: ['Jan', 'Fév', 'Mar', 'Avr', 'Mai', 'Jun', 'Jul', 'Aoû', 'Sep', 'Oct', 'Nov', 'Déc'],
                datasets: [{
                    label: 'Commandes',
                    data: [100, 200, 300, 400, 500, 600, 700, 800, 900, 1000, 1100, 1200],
                    backgroundColor: 'rgba(171, 210, 126, 0.5)',
                    borderColor: 'rgba(171, 210, 126, 1)',
                    borderWidth: 1,
                    borderRadius: 5,
                    barThickness: 40,
                    maxBarThickness: 50
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    },
                    tooltip: {
                        backgroundColor: 'rgba(44, 44, 44, 0.9)',
                        titleFont: {
                            family: "'Poppins', sans-serif",
                            size: 14
                        },
                        bodyFont: {
                            family: "'Poppins', sans-serif",
                            size: 13
                        },
                        padding: 12,
                        cornerRadius: 8,
                        displayColors: false,
                        callbacks: {
                            label: function (context) {
                                return `${context.parsed.y} commandes`;
                            }
                        }
                    }
                },
                scales: {
                    x: {
                        grid: {
                            display: false
                        },
                        title: {
                            display: true,
                            text: "Mois",
                            color: "#2c2c2c",
                            font: {
                                size: 14,
                                family: "'Poppins', sans-serif",
                                weight: '600'
                            },
                            padding: { top: 10 }
                        }
                    },
                    y: {
                        beginAtZero: true,
                        grid: {
                            color: 'rgba(0, 0, 0, 0.05)'
                        },
                        title: {
                            display: true,
                            text: "Nombre de Commandes",
                            color: "#2c2c2c",
                            font: {
                                size: 14,
                                family: "'Poppins', sans-serif",
                                weight: '600'
                            },
                            padding: { bottom: 10 }
                        },
                        ticks: {
                            precision: 0,
                            stepSize: 1
                        }
                    }
                }
            }
        });

        // Update order status
        async function updateOrderStatus(id, status) {
            try {
                const query = `
                    mutation {
                        updateOrderStatus(id: "${id}", status: ${status}) {
                            id
                            status
                        }
                    }
                `;

                await makeGraphQLRequest(query);
                alert('Statut de la commande mis à jour avec succès!');
                window.location.reload();
            } catch (error) {
                console.error('Error updating order status:', error);
                alert('Une erreur est survenue lors de la mise à jour du statut');
            }
        }

        let currentOrderId = null;


    </script>
    <div id="orderDetailsModal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <h2>Détails de la commande</h2>
            <div id="orderDetailsContent"></div>
            <div id="orderMap" style="width:100%;"></div>
        </div>
    </div>
</body>

</html>